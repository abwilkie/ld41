class Hazard {
 constructor(scene) {
    // this.sprite = scene.physics.add.sprite(200, 200, 'player');
    this.scene = scene;
  }

  static preload(scene) {
    // hazard tile?
    // scene.load.image('player', 'assets/octosprite.png', 'assets/player.json');
  }

  static createAnims(){
  }

  create() {
  }

  update() {
  }
}
